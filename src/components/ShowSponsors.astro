---
import kelea from "../assets/logos/kelea.svg";
// base styles
const cardBase =
  "relative mx-auto flex flex-col items-center justify-center overflow-hidden text-center text-amber-300 rounded-lg bg-amber-300/20 backdrop-blur-sm border-2 border-white hover:scale-105 sm:hover:scale-110 hover:bg-yellow-300 hover:text-black hover:shadow-[0_0_20px_4px_rgba(255,255,255,0.8)] transition-all duration-300";

const cautionTape =
  "pointer-events-none absolute left-1/2 top-1/2 z-10 flex h-14 sm:h-16 w-[200%] -translate-x-1/2 -translate-y-1/2 -rotate-12 items-center justify-center bg-[repeating-linear-gradient(135deg,#facc15_0_18px,#111827_18px_36px)] border-y-2 border-black shadow-[0_14px_22px_rgba(0,0,0,0.45)]";

const cautionTapeText =
  "pointer-events-none flex w-full items-center justify-around gap-10 text-xs sm:text-sm md:text-base lg:text-lg font-extrabold uppercase tracking-[0.4em] text-slate-900/90";

const comingSoonOverlay =
  "pointer-events-none absolute left-1/2 top-1/2 z-20 flex w-full -translate-x-1/2 -translate-y-1/2 -rotate-12 items-center justify-center px-4";

const comingSoonOverlayText =
  "whitespace-nowrap flex items-center justify-center rounded-md bg-amber-300 px-3 py-1.5 sm:px-4 sm:py-2 text-xs sm:text-sm md:text-base lg:text-lg font-extrabold uppercase tracking-[0.35em] sm:tracking-[0.4em] md:tracking-[0.45em] text-black shadow-[0_10px_20px_rgba(0,0,0,0.45)]";

const srOnlyText = "sr-only";

const tierHeading =
  "mt-10 sm:mt-12 md:mt-16 text-center font-mono tracking-wider text-lg sm:text-xl md:text-2xl text-amber-300";
---

<section
  class="mt-20 sm:mt-32 md:mt-40 mb-12 sm:mb-16 md:mb-20 max-w-whatever mx-auto px-4 flex flex-col items-center justify-center h-full z-10"
>
  <h1
    id="sponsors-heading"
    class="text-3xl sm:text-4xl md:text-5xl font-black uppercase text-center tracking-[0.25em] text-amber-300 drop-shadow-[0_4px_0_#7c3f00] mb-6 sm:mb-6 md:mb-8"
  >
    Sponsors
  </h1>

  <h3
    id="sponsor-info"
    class="mt-3 sm:mt-5 md:mt-5 text-center text-base sm:text-lg md:text-xl lg:text-2xl tracking-[0.15em] sm:tracking-[0.2em] md:tracking-[0.3em] text-amber-300"
  >
    This wouldn't be possible without them
  </h3>

  <!-- Root -->
  <h3 id="sponsors-root" class={tierHeading}>root</h3>
  <a href="#" class="w-full max-w-md">
    <div
      class={`${cardBase} aspect-[5/3] w-full max-w-[400px] mt-4 sm:mt-6 p-3 sm:p-4 text-lg sm:text-xl`}
    >
      <span class={srOnlyText}></span>
      <span aria-hidden="true" class={cautionTape}>
        <span class={cautionTapeText}> </span>
      </span>
      <span aria-hidden="true" class={comingSoonOverlay}>
        <span class={comingSoonOverlayText}>COMING&nbsp;SOON</span>
      </span>
    </div>
  </a>

  <!-- Admin -->
  <h3 id="sponsors-admin" class={tierHeading}>admin</h3>
  <div
    class="mt-4 sm:mt-6 grid grid-cols-1 sm:grid-cols-2 gap-4 sm:gap-6 md:gap-8 px-3 w-full max-w-4xl"
  >
  <a
      href="https://www.kelea.es/"
      target="_blank"
      class="w-full [--tx:5px] [--ty:0px] [--mx:0px] [--my:0px] [--px:15px] [--py:0px] [--logo-scale:1] [--logo-maxw:100%]"
    >
      <div
        class={`${cardBase} aspect-[5/3] w-full max-w-[320px] p-3 sm:p-4 text-base sm:text-lg md:text-xl`}
      >
        <img
          src={kelea.src}
          alt="Kelea"
          class="mx-auto max-h-[80px] sm:max-h-[100px] object-contain will-change-transform"
          style={"transform:translate(var(--tx),var(--ty)) scale(var(--logo-scale)); margin:var(--my) var(--mx); padding:var(--py) var(--px); max-width:var(--logo-maxw); width:auto; display:block;"}
        />
      </div>
    </a>
    <a href="#" class="w-full">
      <div
        class={`${cardBase} aspect-[5/3] w-full max-w-[320px] p-3 sm:p-4 text-base sm:text-lg md:text-xl`}
      >
        <span class={srOnlyText}>Coming soon</span>
        <span aria-hidden="true" class={cautionTape}> </span>
        <span aria-hidden="true" class={comingSoonOverlay}>
          <span class={comingSoonOverlayText}>COMING&nbsp;SOON</span>
        </span>
      </div>
    </a>
    <a href="#" class="w-full">
      <div
        class={`${cardBase} aspect-[5/3] w-full max-w-[320px] p-3 sm:p-4 text-base sm:text-lg md:text-xl`}
      >
        <span class={srOnlyText}>Coming soon</span>
        <span aria-hidden="true" class={cautionTape}> </span>
        <span aria-hidden="true" class={comingSoonOverlay}>
          <span class={comingSoonOverlayText}>COMING&nbsp;SOON</span>
        </span>
      </div>
    </a>
    <a href="#" class="w-full">
      <div
        class={`${cardBase} aspect-[5/3] w-full max-w-[320px] p-3 sm:p-4 text-base sm:text-lg md:text-xl`}
      >
        <span class={srOnlyText}>Coming soon</span>
        <span aria-hidden="true" class={cautionTape}> </span>
        <span aria-hidden="true" class={comingSoonOverlay}>
          <span class={comingSoonOverlayText}>COMING&nbsp;SOON</span>
        </span>
      </div>
    </a>
        <a href="#" class="w-full">
      <div
        class={`${cardBase} aspect-[5/3] w-full max-w-[320px] p-3 sm:p-4 text-base sm:text-lg md:text-xl`}
      >
        <span class={srOnlyText}>Coming soon</span>
        <span aria-hidden="true" class={cautionTape}> </span>
        <span aria-hidden="true" class={comingSoonOverlay}>
          <span class={comingSoonOverlayText}>COMING&nbsp;SOON</span>
        </span>
      </div>
    </a>
    <a href="#" class="w-full">
      <div
        class={`${cardBase} aspect-[5/3] w-full max-w-[320px] p-3 sm:p-4 text-base sm:text-lg md:text-xl`}
      >
        <span class={srOnlyText}>Coming soon</span>
        <span aria-hidden="true" class={cautionTape}> </span>
        <span aria-hidden="true" class={comingSoonOverlay}>
          <span class={comingSoonOverlayText}>COMING&nbsp;SOON</span>
        </span>
      </div>
    </a>
  </div>

  <!-- User -->
  <h3 id="sponsors-user" class={tierHeading}>user</h3>
  <div
    class="mt-4 sm:mt-6 flex flex-col gap-3 sm:grid sm:grid-cols-3 sm:gap-4 md:gap-6 lg:gap-8 px-3 w-full max-w-5xl"
  >
    <a href="#" class="w-full">
      <div
        class={`${cardBase} aspect-[5/3] w-full max-w-[240px] p-2 sm:p-3 text-sm sm:text-base md:text-lg lg:text-xl`}
      >
        <span class={srOnlyText}>Coming soon</span>
        <span aria-hidden="true" class={cautionTape}> </span>
        <span aria-hidden="true" class={comingSoonOverlay}>
          <span class={comingSoonOverlayText}>COMING&nbsp;SOON</span>
        </span>
      </div>
    </a>
    <a href="#" class="w-full">
      <div
        class={`${cardBase} aspect-[5/3] w-full max-w-[240px] p-2 sm:p-3 text-sm sm:text-base md:text-lg lg:text-xl`}
      >
        <span class={srOnlyText}>Coming soon</span>
        <span aria-hidden="true" class={cautionTape}> </span>
        <span aria-hidden="true" class={comingSoonOverlay}>
          <span class={comingSoonOverlayText}>COMING&nbsp;SOON</span>
        </span>
      </div>
    </a>
    <a href="#" class="w-full">
      <div
        class={`${cardBase} aspect-[5/3] w-full max-w-[240px] p-2 sm:p-3 text-sm sm:text-base md:text-lg lg:text-xl`}
      >
        <span class={srOnlyText}>Coming soon</span>
        <span aria-hidden="true" class={cautionTape}> </span>
        <span aria-hidden="true" class={comingSoonOverlay}>
          <span class={comingSoonOverlayText}>COMING&nbsp;SOON</span>
        </span>
      </div>
    </a>
  </div>
</section>
